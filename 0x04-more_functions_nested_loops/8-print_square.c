#include "main.h"

/**print_square - a function that prints a square, followed by a new line.
*@n:
*Return: 
*/

void print_square(int size)
{
int x, y;
if(size <= 0)
	_putchar("\n");
}
else 
{
for (x = 0; x < sie; x++)
{
foy (y = 0; y < size; y++)
